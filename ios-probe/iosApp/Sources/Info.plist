<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>en</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>QuicProbe</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(MARKETING_VERSION)</string>
	<key>CFBundleVersion</key>
	<string>$(CURRENT_PROJECT_VERSION)</string>
	<key>LSRequiresIPhoneOS</key>
	<true/>
	<key>UILaunchScreen</key>
	<dict/>
	<key>UISupportedInterfaceOrientations</key>
	<array>
		<string>UIInterfaceOrientationPortrait</string>
	</array>

	<!-- Residency. A coroutine `delay` does not keep iOS from suspending the process, so without a
	     background location session the echo loop and the QUIC keepalive both stall the instant the
	     screen locks — which is exactly when the phone is in a pocket recording the walk. -->
	<key>UIBackgroundModes</key>
	<array>
		<string>location</string>
	</array>
	<key>NSLocationWhenInUseUsageDescription</key>
	<string>Keeps the QUIC handoff recording running while you walk. Your location is never stored, sent anywhere, or written to the log — only a count of updates, which is how the recording proves it stayed running in your pocket.</string>
	<key>NSLocationAlwaysAndWhenInUseUsageDescription</key>
	<string>Keeps the QUIC handoff recording running while the screen is locked and the phone is in your pocket. Your location is never stored, sent anywhere, or written to the log — only a count of updates, which is how the recording proves it stayed running.</string>

	<!-- The probe deliberately talks to a plaintext-IP QUIC echo server over UDP; ATS governs TLS
	     over TCP and does not apply, but the key keeps the intent explicit for anyone reading this. -->
	<key>ITSAppUsesNonExemptEncryption</key>
	<false/>

	<!-- So `adb pull`'s iOS equivalent is just the Files app: the walk's log is the deliverable. -->
	<key>UIFileSharingEnabled</key>
	<true/>
	<key>LSSupportsOpeningDocumentsInPlace</key>
	<true/>
</dict>
</plist>
