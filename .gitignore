build
bin
.idea
*.iml
.gradle
*.swp
.project
.kotlin/

kotlinx-io-native/repo/

!.idea/vcs.xml
!.idea/runConfigurations
!.idea/codeStyles

local.properties
.claude/
.vscode
.mcp.json
.gitmodules

# Shell/user config files (shouldn't be in repo)
.bash_profile
.bashrc
.profile
.zprofile
.zshrc
.gitconfig
.ripgreprc

# Built native libraries (built by Gradle)
libs/
# Android JNI shims (built by buildAndroidNativeLibs task)
socket-quic/src/androidMain/jniLibs/*/*.so
socket-quic/src/androidMain/jniLibs/*/.built-*

# Docusaurus
docs/node_modules/
docs/build/
docs/.docusaurus/
docs/static/api/
socket-quic/hs_err_pid*.log

# Generated at build time by the :socket-quic `generateTestP12` task from the
# committed PEM cert+key, for the Apple QUIC server tests (issue #112).
socket-quic/testcerts/cert.p12
socket-quic/testcerts/localhost.p12

# The self-signed `localhost` identity is generated fresh each build by the
# :socket-quic `generateLocalhostCert` task (Apple's 398-day SecTrust cap makes a
# committed cert a guaranteed expiry flake — issue #99). Written to every dir a
# test source set reads from; none are committed.
socket-quic/testcerts/localhost.crt
socket-quic/testcerts/localhost.key
socket-quic/src/jvmTest/resources/certs/localhost.crt
socket-quic/src/jvmTest/resources/certs/localhost.key
socket-quic/src/androidInstrumentedTest/resources/certs/localhost.crt
socket-quic/src/androidInstrumentedTest/resources/certs/localhost.key
